/**
 * Java API contracts and result types that mirror the language-neutral spec.
 *
 * <p>Java runtimes, such as JDT, should expose extracted facts through these
 * contracts. Non-Java runtimes should implement the files under {@code spec/}
 * in their own language instead of depending on this jar.
 */
package com.poseidon.javastatic.extract.runtime;
