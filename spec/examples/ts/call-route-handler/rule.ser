rule "TS Call Route Handler"
fact ts_call_route

find call [get,post,put,patch,delete]
when call owner router

let method =
  from call take method

let path =
  from argument[0] take value

let handler =
  from handler take reference

build {
  method: method | normalize upper
  path: path
  handler: handler
}
